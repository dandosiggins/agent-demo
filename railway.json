{
  "build": {
    "installCommand": "pnpm install --no-frozen-lockfile",
    "buildCommand": "PORT=3000 BASE_PATH=/ pnpm --filter @workspace/agent-demo build && pnpm --filter @workspace/api-server build"
  },
  "deploy": {
    "startCommand": "node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
  }
}